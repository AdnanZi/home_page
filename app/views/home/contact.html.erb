<div class="page-header">
	<h1><%= Constants::CONTACT_TITLE %></h1>
</div>

<div><%= @myInfo && simple_format(@myInfo.content).html_safe %></div>

<% if (loggedIn?) %>
	<div class="page-footer">
		<button class="btn btn-large btn-primary" onclick="window.location = '<%= home_contact_edit_path %>'"><%= Constants::EDIT_CONTACT_BUTTON %></button>
	</div>
<% end %>